# 2026-08-06

- Refined `CrystalGuardian` laser rendering after verifying real `Laser.png` and `Shoot.png` pixels with a throwaway PIL composite before editing Java. Selected the 56px visual / 20px damage-band candidate, enlarged the charged orb to 1.25x and retained it throughout beam hold, moved orb rendering after beam/glow, and added a mirrored 112x68 origin transition cropped from atlas frame 0009 (source columns 130-228) over the uniform body slice. Final `./gradlew :core:compileJava -q` completed cleanly; `git diff --check` also passed.
- Follow-up laser simplification: verified a new real-pixel composite, then halved the beam again to 28px visual / 10px damage height, reduced the retained orb to 0.9x, and removed the separate frame-0009 origin piece so the laser uses only the established frame-4-to-7 body slices from its start. Core compilation and diff checks passed.
- Improved the active charged orb using a real-pixel animation-strip prototype: halved scale again from 0.9x to 0.45x, replaced the frozen frame with a smooth `4,5,6,7,6,5` atlas-frame loop at 0.08s/frame, and added a subtle sine scale pulse (`+/-6%`, speed 7.5) anchored on the measured lamp center. Core compilation and diff checks passed.
- Final Crystal Guardian active-laser polish: detected a concurrent manual orb-scale edit, preserved the intent while replacing the active orb with a generated 4-frame `ChargedOrbClean.png` circular crop (81px source frames, rendered at 52px with the existing pulse), removed the separate `laser_glow` draw that exposed square/fading haze, and made the guardian loop active Shoot.png middle poses `2,3,4,3` during beam hold instead of resting on the final frame. Windup timing remains driven by the original NORMAL shoot animation. Core compile and diff checks passed.
- Corrected the Crystal Guardian shoot sequence after inspecting a numbered contact sheet of all seven `Shoot.png` frames: windup explicitly advances frames 1-3, the laser begins and locks on frame 3 (index 2) without looping, then laser/orb/damage stop and frames 4-7 play once before the guardian turns/walks. Core compile and diff checks passed.
- Follow-up based on in-game observation: moved the active-laser lock to exactly shoot frame 4 (index 3), eliminating any perceived 3/4/5 cycling. Windup plays frames 1-4, frame 4 stays completely static for the full beam, then frames 5-7 play once after shutoff. Core compile and diff checks passed.
